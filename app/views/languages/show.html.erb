<p id="notice"><%= notice %></p>

<table class="table table-dark" style="width: auto;">
    <tr>
      <td><strong>Name:</strong></td>
      <td><%= @language.name.titleize %></td>
    </tr>
</table>

<%= link_to 'Edit', edit_language_path(@language), class: "btn btn-primary text-white" %> |
<%= link_to 'Back', languages_path, class: "btn btn-primary text-white" %>
